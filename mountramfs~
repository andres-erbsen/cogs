#!/bin/bash

location="${HOME}/.ramfs"

if [ x"$(mount | grep $location)" == "x" ];
then 
    mount $location
    rsync -arv $location.save/ $location/
else
    echo $location already mounted, not mounting >&2
    exit
fi


